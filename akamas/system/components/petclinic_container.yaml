kind: component 
system: petclininc

name: petclininc_container
description: container
componentType: Kubernetes Container

properties:
  prometheus:
    namespace: microservices-demo
    pod: petclinic.*
    container: petclinic